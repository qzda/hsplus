# hsplus

快速启动一个静态文件服务器。体积更小，使用更简单。

## 特性

- 支持文本文件预览
- 支持批量下载

## 使用方法

```bash
npx hsplus
# npx hsplus [路径] [选项]
```

## 选项

- `--host` 启用局域网访问
- `-p, --port` 使用端口
- `-h, --help` 显示帮助
- `-v, --version` 显示版本

## 开发

本项目依赖 [`Bun`](https://bun.sh)。

```bash
bun i
bun dev
```
